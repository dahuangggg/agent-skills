# Agent 工作指南

## 重要提醒

- 输出限制：最终回复保持在 500 字以内。代码、配置、PRD 等交付物写入文件，不在回复中展开正文；仅返回文件路径及每个文件的一行描述。若使用了 index 工具，还须列出这些文件对应的 index label。
- 默认使用中文沟通和撰写文档；代码标识符、命令、错误原文保持原样。
- 开始任务前，阅读本文件和 `_session_handoff.md`，再按任务读取相关文档。
- 修改子目录前，检查该目录适用的更具体的 Agent 指令。
- 以实际源码、配置和验证结果为依据。文档与实现不一致时，指出差异并同步修正文档。
- 区分「已实现」「已验证」「待验证」，不得把计划写成事实。
- 保留用户已有改动，不覆盖与当前任务无关的内容。
- 本文件记录长期规则；当前进展、待办和交接信息写入 `_session_handoff.md`。

## 项目信息

| 项目 | 内容 |
| --- | --- |
| 项目名 | {{项目名}} |
| 项目用途 | {{一句话说明核心用途}} |
| 仓库 | {{仓库 URL，或尚未配置远程仓库}} |
| 技术栈 | {{语言、框架、数据库及关键版本来源}} |
| 包管理 | {{包管理器、锁文件、workspace 配置}} |
| 运行环境 | {{运行时及版本要求}} |
| 容器 | {{Dockerfile、Compose 文件、服务；或不适用}} |
| 工作时区 | {{IANA 时区，用于交接日期和归档}} |

## 架构介绍

{{用一段话说明系统边界、主要模块及核心数据流。}}

以下结构应替换为项目实际目录；删除不存在的示例项。

```text
{{项目根目录}}/
├── {{入口目录}}/       # {{职责}}
├── {{核心模块目录}}/   # {{职责}}
├── {{基础设施目录}}/   # {{职责}}
├── {{测试目录}}/       # {{职责}}
└── docs/               # 项目文档
```

核心调用链：

```text
{{入口}} → {{中间件或调度层}} → {{业务模块}} → {{持久化或外部服务}}
```

详细架构见 {{实际架构文档路径；未建立则标记待补充}}。

## 架构约束

### 核心机制

| 机制 | 实现入口 | 必须保持的约束 |
| --- | --- | --- |
| {{中间件／请求管线}} | {{路径}} | {{执行顺序、上下文传播、异常处理方式}} |
| {{认证与授权}} | {{路径}} | {{认证入口、权限校验位置}} |
| {{数据访问与事务}} | {{路径}} | {{访问边界、事务归属}} |
| {{异步任务／事件}} | {{路径}} | {{重试、幂等、失败处理}} |

仅保留项目实际存在的机制。

### 开发新功能

- 新功能放入 {{模块划分规则}} 对应的模块。
- 复用现有的 {{配置、日志、错误处理、认证等公共入口}}。
- 禁止绕过 {{必须经过的中间件、服务层或数据访问层}}。
- 模块依赖遵循 {{允许的依赖方向}}，禁止 {{具体禁止的依赖}}。
- 新增或修改对外接口时，同步更新 {{接口定义与文档位置}}。
- 修改数据结构时，遵循 {{迁移、兼容与回滚要求}}。
- 涉及模块边界、关键依赖或核心机制的决策，记录到 `docs/adr/`。
- 不得把尚未采用的架构方案写成当前架构。

## 文档组织

`docs/README.md` 是文档入口，列出实际存在的文档及用途。
目录按需创建，不预先生成空目录或空文档。

```text
docs/
├── README.md          # 文档导航
├── architecture/      # 当前架构、模块边界、核心流程
├── adr/               # 架构决策及理由、状态、替代关系
├── development/       # 环境配置、开发、测试、调试
├── runbooks/          # 部署、运维、故障排查与恢复
├── api/               # 接口契约、协议与示例
├── plans/             # 尚未完成的方案和实施计划
└── handoff/
    ├── README.md      # 按 ISO 周排列的交接归档索引
    └── YYYY-WW.md     # 对应 ISO 周的原始交接条目
```

文档维护规则：

- `AGENTS.md`：长期有效的工作规则、架构约束和文件入口。
- `_session_handoff.md`：当前状态、当前周记录和下一步工作。
- `docs/architecture/`：已实现的架构事实。
- `docs/adr/`：为何作出某项决策；被替代的决策保留并标记状态。
- `docs/plans/`：未来工作；注明草案、进行中、完成或放弃。
- 同一事实保留一个主要维护位置，其他文档通过链接引用。
- 行为或结构变化时，同步修改受影响的文档和索引。
- 新增、移动、删除文档时，修复相关链接。
- 已有文档布局优先沿用；初始化时将上述分类映射到现有路径，避免重复建档。
- 不在文档中保存密钥、令牌或未经脱敏的敏感数据。

## 关键文件索引

| 文件 | 作用 | 何时阅读或修改 |
| --- | --- | --- |
| `{{应用入口}}` | {{作用}} | {{触发条件}} |
| `{{核心配置}}` | {{作用}} | {{触发条件}} |
| `{{中间件入口}}` | {{作用}} | {{触发条件}} |
| `{{日志配置}}` | {{作用}} | {{触发条件}} |
| `{{测试配置}}` | {{作用}} | {{触发条件}} |
| `{{CI 配置}}` | {{作用}} | 修改验证流程或排查 CI 时 |
| `_session_handoff.md` | 项目状态与交接 | 开始任务、更新进展、提交前 |
| `docs/README.md` | 文档导航 | 定位和维护项目文档 |

仅列关键文件；路径必须存在，计划新增的文件另行标注。

## 开发与验证

命令必须来自实际配置，并写明执行目录。
未验证的命令标记「未验证」；不适用的项目明确写「不适用」。

### 本机运行

| 步骤 | 执行目录 | 命令或说明 |
| --- | --- | --- |
| 安装依赖 | {{目录}} | {{命令}} |
| 配置环境 | {{目录}} | {{环境变量示例文件及配置说明}} |
| 启动依赖服务 | {{目录}} | {{命令或不适用}} |
| 初始化／迁移数据 | {{目录}} | {{命令或不适用}} |
| 启动应用 | {{目录}} | {{命令}} |
| 确认运行状态 | {{目录}} | {{检查方式及成功条件}} |

### Docker 运行

- 前置条件：{{Docker、Compose 及环境配置要求}}。
- 构建与启动：{{实际命令}}。
- 查看状态与日志：{{实际命令}}。
- 停止服务：{{实际命令}}。
- 数据卷与持久化：{{说明；明确哪些操作会删除数据}}。

### 提交与推送前验证

- 按变更范围执行 {{格式检查、静态检查、测试、构建命令}}。
- 后端变更必须执行 {{后端验证命令}}；验证受影响的成功和失败路径。
- 数据迁移变更必须验证 {{迁移及兼容要求}}。
- 推送前完成 {{与 CI 对应的本地检查}}。
- 推送后检查 {{CI 工作流}}；CI 尚未完成时不得报告为通过。
- 无法执行的检查，记录原因、影响和补验命令。
- 纯文档变更按 {{仓库文档检查要求}} 验证。

## 提交规则

- 提交格式：{{仓库现有约定}}。
- 每次 commit 前，同步更新 `_session_handoff.md`，保证架构、进展、待办、关键文件及验证状态与本次变更一致。
- 交接文件和必要的文档更新应随对应变更一起提交。
- 检查暂存区，避免提交无关文件或敏感信息。
- 用户未要求提交或推送时，不因本规则自行执行这些操作。

### 交接条目写入

- 新条目写在 `_session_handoff.md` 的 `## 当前焦点` 最上方。
- 使用三级标题：`### YYYY-MM-DD: 事项摘要`。
- 条目按时间倒序排列；同一天有多个条目时，新条目放在上方。
- 日期按本文件配置的工作时区确定。
- 记录变更、验证、未完成事项和下一步；不复制完整聊天记录。
- 「当前焦点」只放日期条目。常驻状态放在各自章节。

### 按 ISO 周归档

- 根目录 `_session_handoff.md` 的「当前焦点」只保留当前 ISO 周条目。
- 更早的日期条目按所属 ISO 周移入 `docs/handoff/YYYY-WW.md`。
- `YYYY` 使用 ISO 周所属年份，`WW` 使用两位 ISO 周号。使用 `date.isocalendar()` 计算，不使用自然月周次或直接取公历年份。
- 跨周时归档上一周；若遗漏多周，一次补齐所有更早周的条目。
- 只移动「当前焦点」下形如 `### YYYY-MM-DD: ...` 的日期条目。每个条目包含其标题至下一个同级标题或二级标题之前的全部内容；识别标题时排除代码块中的示例。
- 不归档常驻部分：待办、已知阻塞与风险、近期完成、架构速览、关键文件及历史归档导航。
- 归档只搬位置，不改写条目的文字、日期、格式或条目内部顺序。
- 目标周文件已有内容时，保留已有条目，合并本次移动的条目，按时间倒序放置；同日已有条目的相对顺序保持不变。不得覆盖、去重或丢失历史记录。
- 周文件使用 `# YYYY-WW 交接归档` 作为标题，下方直接放原始日期条目。
- 同步更新 `docs/handoff/README.md` 和根目录交接文件的「历史归档」部分，按周倒序链接。周索引使用 `# 交接归档` 标题和指向周文件的相对链接。
- 归档验证：比较移动前后「当前焦点全部日期条目 + 涉及的周归档全部日期条目」的非空行多重集。忽略空白行，但保留非空行原文及出现次数；结果必须一致。文件标题、导航和其他常驻部分不参与比较。
- 先完成并验证纯归档，再追加新条目或修改常驻状态，避免将内容更新混入归档校验。
- 日期不合法、处于未来或归属不明确的条目应报告并保留，不猜测日期。

## 日志规范

- 后端一律禁止使用 `print`，统一使用项目 logger。
- logger 入口：{{路径及使用方式}}。
- 级别约定：{{DEBUG／INFO／WARNING／ERROR 的项目用法}}。
- 异常记录：{{异常堆栈、请求 ID、任务 ID 等要求}}。
- 日志采集与位置：{{handler、采集链路或日志目录}}。
- 不依赖 `print` 的标准输出进入项目日志链路。
- 不记录密钥、令牌或未经脱敏的敏感数据。

## 本地源码参考库

| 名称 | 本地路径 | 用途 |
| --- | --- | --- |
| {{参考库}} | {{绝对路径}} | {{何时参考}} |

- 本地参考库用于理解实现；未经任务授权，不修改参考库。
- 路径不可用时如实说明，不将参考库内容当成本项目当前实现。
- 无参考库时写「未配置」。

## 其他约束与工具偏好

- {{项目特有约束}}。
- 查找文本和文件优先使用 `rg`、`rg --files`。
- 仓库根目录存在 `.codegraph/` 时，理解或定位代码优先使用 `codegraph_explore` 或 `codegraph explore`；未建立索引时跳过，不自行创建索引。
- {{其他工具、依赖管理或工作流偏好}}。
